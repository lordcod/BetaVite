import SVGCommandsMajor from '@/components/5Entities/SVG/Categories/SVGCommandsMajor';
import Category from '../../UI/Category';

export default function CategoriesAllBtn() {
  return (
    <Category
      id='major'
      SVG={SVGCommandsMajor}
    />
  );
}
